# نقطة البداية لـ Claude

هذا **Wassli جديد ونظيف**. لا علاقة له بمشروع Supabase القديم.

## ممنوع تمامًا
- لا تستخدم Supabase.
- لا تضف Supabase.
- لا تطلب مني Project URL أو Keys.
- لا تستخدم `cafnaxybxmdvrwofebza`.
- لا تعيد إدخال migrations أو Auth قديم.

## المطلوب
1. اقرأ المشروع كاملًا.
2. `npm install`
3. `npm run typecheck`
4. `npm run build`
5. شغّل الويب واختبر التسجيل والدخول.
6. أكمل واجهة العميل والمندوب والإدارة والطلبات والتسعير والتقييمات والشكاوى.
7. بعد نجاح الويب فقط جهّز Capacitor/Android.

## Backend
Express + SQLite + JWT + bcrypt.
قاعدة البيانات محلية في `data/wassli.sqlite`.

## التسعير
150 دج حتى 2 كم، ثم +50 دج لكل كم إضافي، والعميل يستطيع اقتراح سعر.
