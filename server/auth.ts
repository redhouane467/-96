import jwt from "jsonwebtoken";import type{Request,Response,NextFunction}from"express";const SECRET=process.env.JWT_SECRET||"change-me-in-production";export type U={id:string;name:string;email:string;phone:string;role:"customer"|"courier"|"admin"};declare global{namespace Express{interface Request{user?:U}}}export function sign(u:U){return jwt.sign(u,SECRET,{expiresIn:"7d"})}export function auth(req:Request,res:Response,next:NextFunction){try{req.user=jwt.verify(req.headers.authorization?.replace("Bearer ","")||"",SECRET) as U;next()}catch{res.status(401).json({error:"يجب تسجيل الدخول"})}}export function role(...r:U["role"][]){return(req:Request,res:Response,next:NextFunction)=>{if(!req.user)return res.status(401).json({error:"يجب تسجيل الدخول"});return r.includes(req.user.role)?next():res.status(403).json({error:"غير مصرح لك بهذا الإجراء"})}}