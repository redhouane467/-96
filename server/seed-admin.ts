import bcrypt from"bcryptjs";import crypto from"node:crypto";import{db}from"./db.js";const email=process.env.ADMIN_EMAIL||"admin@wassli.local",password=process.env.ADMIN_PASSWORD||"Admin123456!";if(!db.prepare("SELECT id FROM users WHERE email=?").get(email)){db.prepare("INSERT INTO users VALUES(?,?,?,?,?,?,?)").run(crypto.randomUUID(),"مشرف وصلي",email,"0000000000",bcrypt.hashSync(password,12),"admin",new Date().toISOString());console.log(email,password)}